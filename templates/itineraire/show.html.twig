{% extends 'base.html.twig' %}

{% block title %}Itineraire{% endblock %}

{% block body %}
    <h1>Itineraire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ itineraire.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ itineraire.titre }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ itineraire.destination }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ itineraire.createdAt ? itineraire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_itineraire_index') }}">back to list</a>

    <a href="{{ path('app_itineraire_edit', {'id': itineraire.id}) }}">edit</a>

    {{ include('itineraire/_delete_form.html.twig') }}
{% endblock %}
