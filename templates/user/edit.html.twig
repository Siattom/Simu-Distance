{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    <div  class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Soumettre') }}</button>
        {{ form_end(form) }}
    </div>

    <a class="pasdelien" href="/user/show/menu">Retour</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
